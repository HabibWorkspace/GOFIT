# GOFIT Gym Attendance Bridge Configuration - LOCAL DEPLOYMENT
# This config is for when Flask runs on the SAME PC as the bridge

# Controller Settings
# IP address of the MC-5924T access controller
CONTROLLER_IP=192.168.1.150

# Port number of the controller (usually 8000)
CONTROLLER_PORT=8000

# Backend Settings
# LOCAL Flask app URL (same machine)
APP_URL=http://localhost:5000

# Secret key for authentication (must match BRIDGE_SECRET_KEY in Flask .env)
SECRET_KEY=change-this-secret-key-in-production

# Timing Settings
# Polling interval reduced to 3 seconds for gate commands
POLL_INTERVAL=3

# Heartbeat interval (in seconds)
HEARTBEAT_INTERVAL=300
